# Vote System for Sinica

using express with sass, ejs, mysql

## dev

remember to create a config/database.js file like this:

```
module.exports = {
  host: '',
  user: '',
  password: '',
  database: ''
};
```

then:

```
npm i
npm i nodemon -g
npm start
```

